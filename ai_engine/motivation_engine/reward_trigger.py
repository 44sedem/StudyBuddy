async def check_and_trigger_reward(student_id: str, completed_task_id: str, db) -> dict | None:
    """
    After a task is completed, check if it has an associated reward.
    Returns reward object if unlocked, None otherwise.
    """
    pass
