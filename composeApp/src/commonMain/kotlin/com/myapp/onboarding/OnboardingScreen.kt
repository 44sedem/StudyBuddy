package com.myapp.onboarding

import androidx.compose.runtime.Composable

@Composable
fun OnboardingScreen(onComplete: () -> Unit) {
    // Conversational step-by-step onboarding flow
    // Steps: AttentionSpan → DailyHours → SleepSchedule → Timetable → Courses
}
